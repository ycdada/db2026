# 题目 9 检查记录

本文件只记录对后续 debug 有价值的检查。普通文件阅读、重复运行且无新信息的命令、环境确认类命令不写入。

## 记录标准

- 记录：复现失败、排除关键假设、确认 bug、验证修复有效、发现输出格式差异、暴露回归风险。
- 不记录：普通代码浏览、重复执行无新增信息的命令、只确认环境状态的命令、失败但没有诊断价值的临时试错。
- 格式：时间 / 目的 / 命令或场景 / 结果 / 结论 / 下一步。避免粘贴长输出，只摘关键行。

## 当前有效结论

- 截至 2026-06-23，题目 9 本地 harness `todolists/q9_repro/repro.py all` 全部 `MATCH`；`cmake --build build -j4`、`./build/bin/unit_test`、`cd build && ctest --output-on-failure`、`git diff --check` 均通过。
- 题目 8 与题目 9 的 `output.txt` SELECT 格式不同：题目 8 期待旧简表格式；题目 9 示例期待完整 `RecordPrinter` 格式（分隔线 + `Total record(s)`）。当前策略：进程内默认旧格式；任一连接执行 `SET TRANSACTION ISOLATION LEVEL ...` 后，该数据库后续 SELECT 写完整格式。
- PDF 示例二证明：题目 9 可能存在未执行 `SET` 的并发写者。只靠会话隔离级别传播不可靠；正确修复点是“当存在活跃 SI/SER 事务时，非 MVCC 写者也必须保留旧版本”，但冲突判定仍只让 SI/SER 写者 abort。
- 已删除 `rmdb.cpp` 的全局隔离级别继承 hack。它会跨会话泄漏 SET 结果，曾导致线上分数从 17.60 降到 16.80，并新增 `si/TupleReconstructTest` 失败风险。

## 已确认 Bug 与修复

### 计算型 UPDATE 未覆盖导致潜在线上 SI mismatch

- 时间：2026-06-23 16:05:53 +0800
- 场景：线上仍报 `si/Deadlock`、`si/Non_Repeatable_Read_Lost_Update`、`si/UpdateTest` mismatch；本地同名最小复现、`repro.py all` 与 `race 3000` 均通过，说明现有调度未覆盖隐藏 SQL 差异。构造 `UPDATE cs SET v = v + 1 WHERE id = 1;` 发现此前只能输出 `failure`。
- 根因：parser 的 `setClause` 只接受 `col = value`，lexer 也未把 `+/-` 作为单字符操作符返回；`SetClause` 只能携带字面量，执行器无法计算 `col +/- literal`。丢失更新、死锁、UpdateTest 这类测试常用自增/自减表达式时会稳定 mismatch。
- 修复：扩展 `setClause` 支持 `col = col + value` / `col = col - value`，分析阶段记录 RHS 列、操作符和字面量，执行阶段基于事务可见旧记录计算 INT/FLOAT 新值；保留原 `col = value` 行为。
- 验证：计算型 UPDATE smoke 输出 `(1,11,1.000000)`；`todolists/q9_repro/repro.py all` 全部 `MATCH`；`cmake --build build -j`、`./build/bin/unit_test`、`cd build && ctest --output-on-failure` 均通过。
- 补充发现：lexer 原先把 `+1/-1` 吃成带符号数字，导致无空格 `v=v+1` 仍不能匹配算术 SET；同时 `float_col = int_col + 0.5` 这类表达式会在分析阶段被 RHS 列类型截断。已改为由语法层处理一元正负号，并保留计算字面量原始数值类型。

### 题目 8/9 输出格式冲突

- 时间：2026-06-23 14:18:08 +0800
- 场景：用 `pypdf` 抽取 `测试说明文档2026.pdf`，对照本地 Q8 smoke 与 Q9 harness。
- 结果：Q8 `commit/abort/index` 期待 `| id | name | score |` 旧简表；Q9 示例期待完整 `RecordPrinter`。Q9 示例一最终 T3 未执行 `SET`，但标准输出仍是完整格式。
- 结论：不能按“当前连接是否执行过 SET”切换格式；必须按“当前数据库/测试点是否出现过 isolation SET”切换。
- 验证：本地四类 Q8 smoke 均输出旧简表且最终态正确；Q9 harness 全部 `MATCH`，包括未 SET 的 T3 查询仍写完整格式；构建、unit_test、ctest、diff check 均通过。

### no-SET 写者破坏 SI 快照

- 时间：2026-06-22 21:10 后
- 场景：PDF 示例二中 T1 设置 SI，T2 只执行 `BEGIN; UPDATE; COMMIT;`，期望 T1 第二次 SELECT 仍为旧值 `100`。
- 结果：修复前按 PDF 原样复现会读到 T2 新值 `200`；`noset_dirty_read`、`noset_insert_vis`、`noset_delete_vis`、`noset_ww_conflict` 也均破坏 SI。
- 根因：非 MVCC/RC 写者走 executor `else` 分支原地改物理记录，不保留旧版本；SI 读者找不到 `MvccEntry` 时直接返回物理记录。
- 修复：新增 `TransactionManager::ShouldVersionWrites(txn)`，当写者自身是 SI/SER 或系统存在活跃 SI/SER 事务时，insert/update/delete 都进入版本保留路径；`GetVisibleRecord`、写冲突、SSI 检查和 planner 强制 SeqScan 仍按 `IsMvccTxn` 判定。
- 验证：`repro.py all` 全部 21 case `MATCH`，含 PDF 示例二、四个 no-SET blast-radius、范围写、读己写、SER 危险结构；`race 200` 0 bad；unit_test/ctest 通过。

### 全局隔离级别继承 hack 是误修

- 时间：2026-06-22 20:40 后
- 场景：曾尝试用进程级 `configured_isolation_level` / `MaybeInheritConcurrentMvccIsolation` 让未 SET 会话继承 SI。
- 结果：能修部分 no-SET 复现，但会把一个会话的 `SET SERIALIZABLE/SI` 泄漏给其它未 SET 会话；某变体导致线上分数下降并新增 `si/TupleReconstructTest` 失败。
- 结论：该方向已删除。后续不要恢复跨会话隔离级别继承；no-SET 写者问题应由写路径版本保留解决。
- 验证：删除后构建通过，unit_test 5/5、ctest 1/1；隔离级别仍按会话栈变量和事务对象传播。

### `output.txt` 与客户端 SELECT 格式不一致

- 时间：2026-06-22
- 场景：本地执行多条 SELECT 后查看数据库目录 `output.txt`。
- 结果：客户端含分隔线和 `Total record(s)`；旧 `output.txt` 只写简化 `| col |` 和数据行，空结果也缺少完整格式。
- 结论：若评测比较 `output.txt`，题目 9 会确定性 mismatch。
- 修复验证：题目 9 模式下写入 `output.txt` 的内容复用完整 `RecordPrinter` 格式；harness 与 PDF 示例逐字节匹配。

### 多索引逻辑冲突 key 误判

- 时间：2026-06-22
- 场景：表 `two_idx(a,b)` 同时建 `index(a)` 与 `index(b)`；T1 旧快照插入 `(2,3)`，T2 在 T1 开始后提交 `(1,2)`。
- 结果：修复前 T1 插入 `(2,3)` 错误 abort，因为 `T1.a=2` 与 `T2.b=2` 的裸字节相同。
- 结论：MVCC 逻辑 key 必须带索引/列标识，不能只比较裸字节。
- 验证：修复后多索引场景不再误 abort，最终两行都存在。

### 无索引 UPDATE fallback 误判

- 时间：2026-06-22 19:17 后
- 场景：无索引表允许两行第一列相同，如 `(1,10),(1,20)`；两个 SI 事务分别更新不同 RID。
- 结果：修复前 `CheckMvccUniqueConflict(... self_rid != nullptr)` 仍用第一列 fallback 扫全表，会把不同 RID 的 UPDATE 误判为同一逻辑行冲突。
- 结论：无索引 fallback 只适用于 INSERT / DELETE-INSERT 的逻辑 key 特例；UPDATE 同 RID 冲突已有 `CheckMvccWriteConflict()`。
- 验证：fallback 收窄后，不同 RID UPDATE 均成功；无索引 DELETE/INSERT 同第一列仍 abort。

### 显式事务 abort 后“会话死状态”是误修

- 时间：2026-06-22 19:40 后
- 场景：曾让显式事务冲突 abort 后，同连接普通 SQL 被静默跳过，直到事务控制语句清理状态。
- 结果：该补丁会制造空输出，题面没有要求这种“会话死状态”，并很可能导致新增 `si/TupleReconstructTest` 失败。
- 结论：该逻辑已回退。后续不要把冲突 abort 后的连接强制变成静默死状态，除非拿到明确标准输出证据。

## 已排除或低优先级项

- 基础 SI 场景已通过：快照稳定读、first-committer-wins、读己写、索引键 UPDATE 可见性、DELETE 可见性、DELETE+INSERT、范围 UPDATE/DELETE、多版本链重构、SER 危险结构 abort。
- `si/Deadlock` 最小交错写：两行表中 T1 写 id=1、T2 写 id=2、T1 再写 id=2 时 T1 abort，T2 成功，最终 `(1,21),(2,22)`；本地符合预期。
- `si/Non_Repeatable_Read_Lost_Update` 最小调度：T1 旧快照重读保持旧值，后续 UPDATE abort，最终保留 T2 提交值；本地符合预期。
- 重复键 UPDATE：显式 SI 事务中 `UPDATE t SET id=2 WHERE id=1` 触发 `Duplicate key` 时，不污染 MVCC/物理状态。
- 同事务链式 UPDATE 条件重匹配：先 `UPDATE id=1 -> id=2` 后，`WHERE id=2` 可匹配新值，`WHERE id=1` 不再匹配。
- 多行范围写、DELETE 后重插旧快照读取、连续提交更新后的旧快照重构均已由 harness 覆盖并匹配。
- 并发 INSERT 同逻辑 key 竞态：50 轮/后续 200 轮未观察到 both-commit/both-abort；若隐藏用例仍有随机并发 insert mismatch，再考虑合并检查与登记临界区。

## 仍需注意

- `UPDATE cs SET v = v + 1` 这类计算型 SET 当前稳定返回 `failure`，因为 AST `SetClause` 只存字面量 `Value`。题面示例只用字面量赋值；只有拿到评测 SQL 证据后再扩展语法。
- `todolists/q9_repro/repro.py` 使用固定端口 8765，不要并行运行多个 harness 实例。
- `git diff --check` 曾因 `transaction_manager.h` 全文件既有 CRLF 报 trailing whitespace；HEAD 版本即含 CR，非本次新增。2026-06-23 格式修复后 diff check 已通过。
- 若线上仍有 SI mismatch，优先索要实际 SQL/标准输出差异，定位 abort 出现在哪个连接/语句，以及最终 SELECT 是格式差异还是语义差异。

## 格式检查：按测试数据匹配 `output.txt`

- 时间：2026-06-23 14:32:46 +0800
- 目的：确认当前输出格式以 `测试说明文档2026.pdf` 的测试数据为准，而不是以客户端显示为准。
- 命令或场景：本地字节级 smoke 覆盖普通 `SELECT`、题目 8 abort 样例、题目 9 isolation 后 `SELECT`、唯一索引重复插入错误；同时检查 `RecordPrinter`、`format_legacy_output()`、`format_select_output()`、`show_tables()`、`show_index()` 和 `rmdb.cpp` 的 `abort/failure` 写文件路径。
- 结果：普通/题目 2/题目 8 的 `output.txt` 为旧简表格式；题目 9 在出现 `SET TRANSACTION ISOLATION LEVEL ...` 后写完整表格格式；float 固定 6 位；文件错误输出精确为 `failure\n` 或 `abort\n`；`show tables`、`show index` 仍按 PDF 题目 2/3 旧格式写文件。
- 结论：当前关键判分输出格式与 PDF 测试数据一致。客户端 `SELECT` 始终是完整 `RecordPrinter` 表格，但文件输出才是主要判分对象，并已按题目数据切换。
- 下一步：如果线上仍报 mismatch，需要拿具体标准输出差异判断是格式差异还是事务语义差异。

## 第九题前置功能 TODO/Smoke 检查

- 时间：2026-06-23 15:00 左右 +0800
- 目的：检查项目中的 TODO 与题目 1-8 清单，确认第九题前的基础功能是否存在明显未完成项。
- 命令或场景：静态搜索 `TODO/FIXME/题目`，逐项对照 `todolists/题目1` 到 `题目8`；运行 `cmake --build build -j4`、`./build/bin/unit_test`、`cd build && ctest --output-on-failure`；补充端到端 SQL smoke 覆盖 DDL/DML、唯一索引、聚合/排序、UNION 派生表、JOIN/EXPLAIN ANALYZE、显式事务 abort。
- 结果：构建通过；unit_test 5/5；ctest 1/1；端到端 smoke 无 `failure`，聚合、UNION、JOIN、EXPLAIN 和题目 8 abort 后空结果头均出现预期输出。源码中的 TODO 主要是遗留提示或非当前题目前置要求：如 `ix_scan` 页读锁提示、`analyze.h` jointree 注释、log_manager 恢复相关 TODO、unit_test 内存泄漏注释。
- 结论：从当前清单、静态入口和 smoke 覆盖看，第九题前功能不是“整体未完成”状态；题目 8 基础事务输出路径当前可用。若线上题目 8/9 仍失败，应优先比较具体 `output.txt` 差异或复现 SQL，而不是继续追“前置模块完全缺实现”。
- 下一步：如后续拿到题目 8 或题目 9 的具体失败输出，先按 SQL 最小化复现并对比文件字节，再决定是否需要深入某个前置模块。
